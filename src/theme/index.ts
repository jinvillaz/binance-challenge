import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    mode: 'dark',
    info: { 
      main: '#0ECB81',
      dark: '#F6465D',
      light: '#a7a7a7',
      contrastText: 'rgb(240, 185, 11)',
    }
  },
});
